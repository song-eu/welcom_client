import { createGlobalStyle } from 'styled-components'
import bitro_core from './비트로 코어 TTF.ttf'

export default createGlobalStyle`
    @font-face {
        font-family: bitro;
        src: 
        url('./비트로 코어 TTF.ttf') 
    }
`
